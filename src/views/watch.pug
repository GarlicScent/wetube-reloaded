extends base.pug

block content 
  video(src="/" + video.fileUrl width=700 controls)  
  div 
    p=video.description
    small=video.createdAt
  a(href=`${video.id}/edit`) Edit Video &rarr;
  br
  a(href=`${video.id}/delete`) Delet Video &rarr;